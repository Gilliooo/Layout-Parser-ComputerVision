# Pull PyTorch from the CPU-only index so Streamlit Cloud doesn't try to
# download the multi-GB CUDA wheels (they blow past the cloud disk/RAM limits).
--extra-index-url https://download.pytorch.org/whl/cpu

# Web app
streamlit==1.57.0

# Core
numpy==2.4.4
Pillow==12.2.0
opencv-python==4.13.0.92

# Deep learning (CPU-only builds)
tensorflow-cpu==2.21.0
torch==2.11.0+cpu
torchvision==0.26.0+cpu

# Layout detection
doclayout-yolo==0.0.4
huggingface-hub==1.12.0

# Image processing
scikit-image==0.26.0
imutils==0.5.4
